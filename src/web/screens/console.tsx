import { ScreenShell } from "./_placeholder";

export function ConsoleScreen() {
  return (
    <ScreenShell
      num="04"
      title="Console"
      subtitle="Novelty, source mix, daily cap, queue ceiling, retrain cadence."
    />
  );
}
