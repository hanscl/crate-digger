import { ScreenShell } from "./_placeholder";

export function SetupScreen() {
  return (
    <ScreenShell
      num="06"
      title="Setup"
      subtitle="Connect Spotify, optionally seed buckets from a playlist URL."
    />
  );
}
